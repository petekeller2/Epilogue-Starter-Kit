// let srcOrBuild;
// if (process.env.NODE_ENV === 'staging' || process.env.NODE_ENV === 'production') {
//   srcOrBuild = 'build';
// } else {
//   srcOrBuild = 'src';
// }
// import assert from 'assert';
//
